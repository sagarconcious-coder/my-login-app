{
  "name": "my-login-app",
  "version": "1.0.0",
  "description": "Login app with React and Node.js",
  "scripts": {
    "build": "cd frontend && rm -rf node_modules/.cache && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "20"
  }
}
